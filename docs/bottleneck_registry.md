# Dependency and bottleneck registry

This registry tracks the shortest path to the required verified `p125`
certificate.  Measurements refer to the Apple M4 local host unless stated
otherwise.  A benchmark result is not a completion claim; the authoritative
completion gate remains acceptance by the unmodified pinned `voneshot.py`.

## Outcome gates

| Gate | State | Required evidence |
|---|---|---|
| Newly searched `p125` certificate | **open** | Durable canonical certificate, identity-bound checkpoint/log, and local pinned-verifier `True` transcript |
| Reproducible production search | **open** | Clean-checkout command and retained range/seed/build/cache/table identities on the winning run |
| `p130` continuation | waiting on `p125` | Same implementation and artifact discipline after all mandatory `p125` gates close |

## Active bottlenecks and dependencies

| Priority | Item | Evidence / decision | Next gate |
|---|---|---|---|
| P0 | SEA cost per admitted curve | Exact packed convolution now supports a reusable reverse-polynomial reducer for monic quotient degrees at least 96. Against the same-source packed-convolution build with reciprocal reduction disabled, B/S/S/B p125 replay improved the full SEA stage 1.32786x and degree-194/401 Frobenius 1.77482x/3.21404x; all projections were identical. The earlier isolated RunPod 30-curve production-path probe took 800.92 s, 7,150.64 user s, and 10.81 GB peak RSS: 26.7 elapsed seconds/curve including cache startup, 30 sound rejections, 14 full counts, and no fallback levels. A later smooth-coordinator candidate still has only one indicative 1.0886x same-range result; its repeat was contaminated and is not promotion evidence. | Re-run a bounded multi-curve production-path probe with reciprocal reduction to measure realized curve throughput and resource use. Separately run a clean bracketed B/A/A/B coordinator gate on an isolated host; require strict `>1.05x` with identical non-timing projections before changing coordinator policy. |
| P0 | Production certificate search | The frozen local `43ca2d7` sound-only X1(27) worker durably advanced from 247 through 658: 412 curves attempted, 412 sound smoothness rejections, 209 full point counts, zero heuristic skips, and zero certificates. The authenticated RunPod probe then exhausted disjoint `[1000000,1000030)` with 30 sound rejections and zero certificates. First production launch `p125-runpod-cpu16-prod-20260802a` authenticated the cache but processed zero curves because the launcher incorrectly documented `max-curves=0` as unlimited; its unchanged cursor and no-op artifact are retained. The launchers now require a positive cap even with a wall timeout. Replacement `p125-runpod-cpu16-prod-20260802b` uses the assigned positive cap and, by `2026-08-02T18:56:44Z`, had durably committed indices `1000030` and `1000031` with checkpoint `1000032`. | Supervise the live replacement under its 14,400-second/$2.57 boundary. Fetch immediately on a candidate or at the cap; keep fallback on, incomplete skip off, and the unproven coordinator disabled. |
| P1 | Search parallelism | Commit `f8347c6` adds a rolling ordered curve window against one immutable 5.4 GB smooth cache. Same-binary K=1/2/3/5/10 scaling reached 100.175/57.800/40.501/34.185/27.071 warm seconds per curve. K=10 reported 6.19 GB RSS and 68.9 MiB of system-wide swapouts; all overlapping canonical records match. | Use `curve_threads=10, sea_threads=1, smooth_threads=1` with memory telemetry; do not exceed the ten physical cores without a new bounded A/B |
| P1 | Certificate opportunity model and family selection | A same-binary reverse-order gate averaged 197.93 s for X1(27) versus 215.10 s for X1(11), or 1.08675x observed wall throughput. Each family has ten deterministic observations; every run found zero certificates. Separately, cyclic divisors 108 versus 44 model 1.068973x smooth-order opportunity, or 1.16170x when combined with measured throughput. | Promote X1(27) point-four with cap 16 for the next production identity; monitor the longer sample and retain the observed-versus-modeled distinction |
| External | Cloud capacity | Direct RunPod SSH, immutable v3 worker launch, exact cache construction, artifact retrieval, and a bounded production-path run are verified on a 16-vCPU/64-GB CPU pod at `$0.643/hour`. The full p125 cache reproduced the trusted 5,400,760,038-byte digest. The tagged AWS trial is terminated and AWS has no active resources; its `m8g.xlarge` was much slower than both local and RunPod. | Supervise the capped RunPod shard and stop promptly after artifact retrieval. Keep AWS compute off unless isolated capacity is needed and a measured instance clears the existing speed/cost gate. |
| External | Local Magma launcher | The direct Magma V2.29-1 runtime/root is healthy but outside `PATH`. It completed the source-bound 10,000-curve Weber corpus through 32 bits with full runtime/package/library identity closure. | Continue passing the explicit runtime/root paths to mandatory oracle gates; production remains CAS-independent |

## Resolved or bounded items

| Item | Resolution evidence |
|---|---|
| Full-cache smoothness memory | Commit `6538bc7`; real 5 GB cache extraction completed with exact known outputs, 5.55 GB maximum RSS, zero swaps |
| Certificate-incompatible curve generation | The exact j-to-Montgomery admission predicate rejects the old p125 index-0 curve before SEA. A `p=10093` census admitted 50.89%; a 32-index p125 probe needed 41 rejected attempts in 0.48 s, making every scheduled SEA curve assembly-eligible. |
| Implementation-limit cursor safety | `sea_level_limit` and `no_rational_weber_lift` checkpoint/report without advancing by default; the explicit skip remains heuristic and schedule-bound. Indices 206 and 246 now have corrected frozen final-binary recoveries. At 246, v1 correctly stopped without advancing and remains unchanged; the final v2 replay recovered it soundly through ell 37 and closed the gate. |
| Early trace-cap policy | The general default remains 64: old Weber cap-195/4096 experiments were negative. On the new X1 trace-prior path, same-build caps 64/32/16/1 took 272.19/271.95/260.34/260.30 s for indices 12--21. Cap 16 cut trace screens 97 to 24 and concurrent curve work 1678.006 to 1435.541 s; cap 1's 0.04 s wall edge is noise and its 1451.021 s curve-work sum is 1.08% worse. Production explicitly selects 16. Index 206 proves that some curves remain incomplete even above cap 64 after all tables; the opt-in skip is explicitly heuristic and schedule-bound. |
| SEA task fan-out | Modular-root jobs are capped by `--sea-threads` and the source-lift count; the serial and bounded-parallel paths are differentially tested and report actual per-level worker counts. |
| Cache trust/completeness boundary | Every pre-existing exact cache requires a trusted lowercase SHA-256; p125 cache digest is pinned in `data/smooth_cache/TRUSTED_MANIFEST.json` |
| Candidate divisor coverage | Exhaustive bounded DFS tries every admissible divisor while failing closed on candidate/node caps |
| Final acceptance | Native validation is followed by the unmodified pinned canonical verifier under an authenticated Python 3 runtime |
| First production Atkin constraints | Pinned classical `Phi_5`/`Phi_7` factor degrees are used only with monic, square-free, uniform-degree no-root evidence; Atkin state is separate from the exact unique-trace gate and the progress auditor independently reconstructs it |
| Weber table trust boundary | Production startup pins the checked-in manifest digest and verifies the complete table filename set, byte counts, and every SHA-256 before SEA; tests reject missing, extra, and altered tables |
| Weber 24th-root orbit reuse | Covariance is verified term-by-term from each loaded table; a controlled off/on ablation and a 64-level retained production replay have identical canonical mathematical projections |
| Conjugate Frobenius reuse | A same-binary off/on ablation and full production replay partition every validated kernel into independent/derived telemetry; 42- and 64-level canonical projections are identical, including scalar/non-scalar safety tests |
| Rolling shared-cache curve execution | One immutable exact-smooth engine is used concurrently; durable state remains ordered and lower-index implementation limits/certificates discard higher speculative reports. The resource setting is resumable and absent from semantic identity. |
| Exact family trace priors | Weber-f claims `t=p+1 (mod 4)` only after direct full-rational-`E[2]` validation. X1(11) seeds `t=+(p+1)` or `-(p+1)` modulo the selected group divisor 44/88, promoted to 176 for the proven point-four `p=5 mod 8` branch, and skips redundant `ell=11`. The policy is bound into the schedule digest and emitted per curve. |
| Generator-retained Weber source | Both curve generators retain the exact nonexceptional Weber-f point. Strict validation precedes singleton SEA; unknown-source callers remain exhaustive and pre-policy checkpoints are incompatible. The one-orbit p125 control was neutral, while the three-orbit index 17 reduced SEA 129.163 to 59.823 s and modular roots 100.457 to 33.537 s with every exact projection unchanged. |
| Reusable reciprocal reduction | The pre-Kronecker prototype was correctly rejected as 14.31% slower. Reimplemented over packed convolution with one inverse amortized per `powmod`, it now passes independent long-reducer differentials through degree 401 plus ASan/UBSan. Same-source B/S/S/B improves degree-194/401 Frobenius 1.77482x/3.21404x and full SEA 1.32786x. A threshold-48 full replay was neutral versus 96 and regressed at degree 48, so production conservatively activates at degree 96. |
| Deferred product normalization | The quotient reducer already normalizes every raw convolution coefficient at its pivot or final output. Removing the redundant pre-pass was exact across adversarial non-monic/negative/alias cases and improved paired p125 index-17 SEA by 1.03479x, modular roots by 1.03840x, and eigen recovery by 1.04723x. |
| Exact-cost polynomial exponent window | Widths one through five are costed including odd-power-table construction, so the binary-width plan remains available for small or sparse inputs. Tests through every exponent 0--1024 and all p125 Frobenius exponents cover nonmonic and repeated-factor quotients without inverses. Reverse-order p125 pairs improved mean SEA 57.088 to 49.178 s (1.16085x) and modular roots 32.007 to 24.013 s (1.33293x), with identical 55-level projections and final trace. |
| Polynomial-subring Element powers | The production eigenvalue bases `x` and `f` have zero y-coordinate and stay in the polynomial subring, while nonzero-y callers retain the general binary loop. Differential tests cover fixed, random, sparse, reducible, repeated-factor, constant-modulus, and general-Element cases. Reverse-order p125 pairs improved isolated level-269 eigen recovery by 1.14084x, full eigen recovery by 1.17333x, and full SEA by 1.05493x with exact projections unchanged. |
| Specialized polynomial square | The schoolbook base case computes one diagonal plus one doubled cross product, while recursion uses three exact squares. Differential tests cover coefficient counts 1--195 around every threshold, nonmonic/repeated moduli, high-degree pre-reduction, adversarial coefficients, zero, alias, and constant cases. Reverse-order p125 pairs improved roots 1.08899x, SEA 1.07060x, and wall 1.06837x with identical 55-level projections and final trace. |
| In-place polynomial-square cross terms | The same exact square identity now accumulates undoubled cross terms with `mpz_addmul`, doubles each output coefficient once, and adds diagonals afterward. Reverse-order same-input p125 level-193 runs improved wall 1.07836x, roots 1.08654x, and eigen recovery 1.08973x; all 42 non-timing projections matched. This is bounded SEA evidence, not a full-search throughput or yield claim. |
| Packed polynomial convolution | Exact limb-aligned Kronecker substitution handles balanced products and squares from 48 coefficients, with Karatsuba retained below the measured crossover. Independent differentials extend through degree 401 and pass ASan/UBSan. Same-source B/S/S/B p125 runs improved degree-194 and degree-401 Frobenius 1.16986x and 1.17477x, and the full SEA stage 1.16529x; all full projections had SHA-256 `8055a435d1abd535574867a55169168635ac683c2ed9e065df135d7440f4b8e6`. Proof, timings, identities, and limitations are in `docs/kronecker_convolution.md`. |
| Odd-degree norm eigenvalue sign | Dense-resultant recovery improved the targeted subtotal 1.52646x but missed the strict wall gate at 1.04907x. A Gauss-lemma follow-up removed the dense Jacobian-coordinate resultants and improved the target 1.56006x, yet fresh B/S/S/B wall improved only 1.036277x with all projections unchanged. Both variants are rejected; this route is closed. |
| X1(11) family throughput gate | Same-build indices 12--21 measured 1.03945x X1 point-four wall throughput over Weber (294.57 versus 306.19 s) with ten sound rejections and zero certificates per family. This clears the predefined 1.1775 modeled-opportunity penalty threshold without claiming observed yield. |
| X1(27) family throughput gate | Same-binary reverse-order repetitions on indices 0--9 measured 1.08675x X1(27) point-four wall throughput over X1(11) (197.93 versus 215.10 s mean). Every run had ten sound rejections, six full counts, zero heuristic skips, and zero certificates. The separate cyclic-divisor model raises the combined planning rate to 1.16170x without claiming observed yield. |
| Production Weber/Magma corpus | Clean commit `dc79fbd` produced 10,000 source-bound records across 16/20/24/28/32-bit primes. Independent replay trial-factored 107,220 orders, found 122 sound rejections and 14 pre-unique-trace saves, and found zero sound false negatives; the explicit fallback-off heuristic skipped zero corpus curves. Compact hashes and commands are in `artifacts/local/weber-oracle-v2-10000-20260802/result.json`. |
| Required performance ablations | Early abort, batching, curve/twist sharing, specialized Weber work, and prime scheduling are quantified. The held-out measured schedule was 0.7% slower than increasing order; the common-level Weber/classical boundary is honestly negative, while no classical 77-level production schedule exists. |

## Measurement discipline

- Do not run a 5 GB cache benchmark concurrently with an SEA timing run.
- Resource-limit outcomes are implementation evidence, not mathematical
  rejection evidence.
- Search ranges are global half-open intervals partitioned exactly once by the
  production CLI; all workers in one partition share the same seed and worker
  count.
- Commit and push each tested milestone before deploying it or changing the
  live search identity.
